# AGENTS.md

在 `com.gthbj.tracking` 干活的通用协议。**只写这个仓自己的规矩**：东西放哪、git 怎么用、什么该记。

> 同一条事实存两份，改一处另一处立刻变错。本文件不复述下面这四处的内容，用到就去读：
>
> | 要什么 | 去哪 |
> |---|---|
> | 包里有什么、消费方怎么接、接入时的坑（EDM4U 不扫包目录、R8 keep 规则…） | `README.md` |
> | 模块边界判据、破坏性变更纪律、Android 程序集只有 APK 能编译、测试怎么被带起来 | `CLAUDE.md` |
> | 定过什么、为什么、代价 | `DECISION_LOG.md` |
> | 设计文档与 PRD | `Docs~/` |

## 一、新增的东西放哪

| 东西 | 去处 |
|---|---|
| 新的域模块（同意、广告、IAP…） | `Runtime/<域>/`，自己的 asmdef，只引 `Tracking` 核心 |
| 设计文档 / PRD | `Docs~/`，命名 `PRD_YYYYMMDD_HHMM_主题.md`（`HHMM` 取创建时刻，出生即无撞） |
| 不可逆取舍、破坏性变更、模块边界口径 | `DECISION_LOG.md`，**最新在上** |
| 消费方要做的事（依赖声明、proguard、装配根） | `README.md` 的接入一节 |

🔴 **`Docs~/` 的 `~` 是有意的**：Unity 不导入以 `~` 结尾的目录，设计文档因此不会变成消费方工程里的资源。

## 二、Git

- **分支 + PR 合 `main`，不直推**（本仓至今全部改动都走 PR）。
- 提交信息写**结论**而不是「更新文档」；破坏性变更用 `!`（如 `feat(core)!:`）。
- 提交信息末尾带模型署名 trailer（`Co-Authored-By: <模型名> <noreply@…>`）；
  自己写 / 大改的文档在文末加一行 `> 文档维护：<模型名>（日期）`，追加在既有那几行前面，不覆盖前人的。
- 本仓**没有 CI**：门禁只能由消费方跑（见 `CLAUDE.md` 的测试一条）。改完 Android 侧尤其别拿
  「本仓看起来没问题」当证据。

## 三、什么该记进 `DECISION_LOG.md`

判别式是**这条会不会约束将来的工作**：改名、撤回接口、模块边界、「照某个竞品做」这类口径——记。
某次实现怎么写的、某个 bug 怎么修的——不记，那些在提交信息与 PR 里。

## 四、不写会过期的状态

`README.md` / `CLAUDE.md` / `AGENTS.md` 这三份**活跃**文件里不写当前 HEAD 的 SHA、消费方钉的版本、
测试通过数、PR 状态这类会过期的事实——改写成指针或现取命令。`DECISION_LOG.md` 与 `Docs~/` 不受此限：
那里记的是**当时**的决定与证据，本来就该冻住。

> 文档维护：Claude Opus 5（2026-09-11 建档）
